def run(session):
    # Production would join classify-sentiment's and extract-entities'
    # output tables on ticket key.
    return "aggregate-report: joined sentiment + entity signals into ticket_enrichment_summary"
