# Snowflake-side config for register_procedures.py / deploy_task_graph.py.
dag_name: support_ticket_enrichment
database: tutorial_db
schema: data_schema
# Must be a permanent stage, not Snowpark's default temp stage -- the
# registering session ends but the tasks keep firing long after.
stage: sproc_task_graph_stage
